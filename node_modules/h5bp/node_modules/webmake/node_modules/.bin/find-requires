#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../find-requires/bin/find-requires" "$@"
  ret=$?
else 
  node  "$basedir/../find-requires/bin/find-requires" "$@"
  ret=$?
fi
exit $ret
